export default interface myEventInterface {
  id: string;
  title: string;
  date: string;
  image?: string;
  form: string;
  save?: boolean;
  visited?: boolean;
  rate?: number;
  info?: string;
  location?: string;
  org?: string;
  source?: string;
}
